import { defineComponent } from "vue";

export const SittingView = defineComponent({
  name: "Sitting",
  setup() {
    return () => (
      <main class="w-full h-full flex flex-col">
        <div></div>
      </main>
    );
  },
});
